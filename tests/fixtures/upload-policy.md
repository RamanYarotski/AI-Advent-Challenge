# Upload Policy Fixture

Uploaded knowledge packs are accepted by the RAG Source Manager when they use a supported document extension. This fixture is intentionally longer than eighty characters so the indexer keeps it as a usable document during tests and demos.
